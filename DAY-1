// to calc the sum of first N numbers
#include <stdio.h>
int main ()
{
    int N , i , sum = 0 ,r ; 
    printf("Enter the value of N: ");
    scanf("%d", &N);
    for(i = 1; i <= N; i++)
    {
        sum = sum + i;
    }
    printf("Sum of first %d numbers is %d", N, sum);
    return 0;
}
// to print multiplication table of a number
int main ()
{
    int n , i = 1 ;
    printf ( "Enter a number : " ) ;
    while ( n <= 10 )
    {
        printf ( "%d * %d = %d "  , n , i , n*i ) ;
        i++ ; 
    }
    return 0 ; 
}
